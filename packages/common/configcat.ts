// ConfigCat external service removed - return empty flags
export async function getFlags(userEmail: string = '', customAttributes?: Record<string, string>) {
  // External service dependency removed
  return []
}
