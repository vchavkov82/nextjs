{
  "// Environment": "OpenSUSE with Podman",
  "environment": {
    "os": "opensuse",
    "container_runtime": "podman",
    "compose_command": "podman compose"
  },
  "// Container Runtime": "Always use podman compose, never docker or docker-compose",
  "container": {
    "runtime": "podman",
    "compose_command": "podman compose",
    "compose_file": "docker-compose.yml"
  },
  "// Script Management": "When creating scripts in feature branches, also commit them to develop",
  "scripts": {
    "auto_commit_to_develop": true,
    "script_directory": "scripts"
  },
  "// Development Commands": "Prefer podman compose over docker-compose",
  "commands": {
    "compose_up": "podman compose up",
    "compose_down": "podman compose down",
    "compose_ps": "podman compose ps",
    "compose_exec": "podman compose exec"
  },
  "// Notes": [
    "User regularly rebases develop into feature branches",
    "Scripts created in feature branches should be committed to develop as well",
    "Never use docker or docker-compose commands",
    "Always use podman compose"
  ]
}
