# Build optimization settings for scaling builds
# Adjust based on your available CPU/RAM

# Node.js memory limit (8GB - adjust as needed)
NODE_OPTIONS=--max-old-space-size=8192

# Turbo settings
TURBO_REMOTE_ONLY=true
TURBO_LOG_ORDER=stream

# Next.js build concurrency
NEXT_CONCURRENT_FEATURES=true

# Disable source maps in production to speed up builds
NEXT_SOURCEMAPS=false
